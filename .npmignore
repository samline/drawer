# Source & tests
src/
test/

# Build configs
tsconfig.json
tsup.config.ts
vitest.config.ts

# Docs (shipped docs go in docs/ alongside the published tarball via `files: ["dist"]`)
# When the package is published from the dist/ output alone, this entry is moot,
# but the npm pack step still walks the working tree to evaluate these patterns.
docs/

# Tooling & lockfiles
.github/
bun.lock
bun.lockb
package-lock.json
pnpm-lock.yaml
yarn.lock
skills-lock.json

# Editor / OS junk
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# AI agent instructions (versioned in git, NOT in the npm tarball)
AGENTS.md
.agents/
